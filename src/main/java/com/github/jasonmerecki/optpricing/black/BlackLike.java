package com.github.jasonmerecki.optpricing.black;

public class BlackLike {

	public static double d1(double s, double k, double r, double q, double tyr, double sigma) {
		return 0.0d;
	}
	/*
	Function BSDOne(S, X, r, q, tyr, sigma)

	'   Returns Black-Scholes d1 value
	    BSDOne = (Log(S / X) + (r - q + 0.5 * sigma ^ 2) * tyr) / (sigma * Sqr(tyr))
	    */
}
